version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8
    steps:
      - checkout
      - run: echo "I'm alive"
  test:
    docker:
      - image: circleci/node:8
    steps:
      - checkout
      - run: npm test
      - store_test_results:
          path: coverage/clover.xml

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test